<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorWhite</item>
        <item name="toolbarStyle">@style/MyToolBarStyle</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--充分利用状态栏，让状态栏跟背景图片融合我们需要把图片和他的所有父控件都要设置一个属性：
    android: fitsSysytemWindows="true"
    然后还必须在程序的主题中将状态栏的颜色设置成透明的才行,然后别忘了在AndroidManifest.xml中
    给FruitActivity设置FruitTheme

    这里的FruitActivityTheme主题内不是空的，因为Android 5.0之前的系统无法指定状态栏的颜色，
    在 values-v21/styles.xml中设置的是Android 5.0及以后的系统都可以指定状态栏的颜色-->
    <style name="FruitActivityTheme" parent="AppTheme">
    </style>

    <style name="netease_toolbar_subtitle" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Subtitle">
        <item name="android:textColor">#eeeeee</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="netease_toolbar_title" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">17sp</item>
    </style>
    
    <style name="MyEditTExt" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorControlNormal">@color/colorDarkWhite</item>
        <item name="android:colorControlActivated">@color/colorDarkWhite</item>
    </style>
    
    <style name="MyToolBarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="titleMargin">0dp</item>
        <item name="contentInsetStart">0dp</item>
    </style>


</resources>
